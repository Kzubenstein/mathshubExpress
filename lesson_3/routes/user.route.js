const {Router} = require("express");
//десструктуризация
const router = new Router();

router.get('/, () => {}');
router.post('/, () => {}');
router.put('/, () => {}');

module.exports = router;